═══════════════════════════════════════════════════════
    HOW TO VIEW YOUR APP IN DOCKER
═══════════════════════════════════════════════════════

YOUR APP IS ALREADY RUNNING! ✅
───────────────────────────────────────────────────────

METHOD 1: VIEW IN BROWSER (EASIEST)
───────────────────────────────────────────────────────
1. Open any browser (Chrome, Edge, Firefox)
2. Type in address bar: localhost:3000
3. Press Enter
4. YOUR APP WILL APPEAR! 🎉

METHOD 2: VIEW IN DOCKER DESKTOP
───────────────────────────────────────────────────────
1. Open Docker Desktop (from Start Menu)
2. Click "Containers" tab (on the left)
3. You'll see "pdf-merger-app" listed
4. Click on it to see:
   - Logs (what's happening)
   - Stats (CPU, memory usage)
   - Settings

METHOD 3: VIEW USING POWERSHELL COMMANDS
───────────────────────────────────────────────────────
Open PowerShell, then:

Check if running:
   docker ps

See logs:
   docker-compose logs --tail=20

See live logs (press Ctrl+C to stop):
   docker-compose logs -f

═══════════════════════════════════════════════════════
    DEPLOYMENT STATUS
═══════════════════════════════════════════════════════

✅ Container Name: pdf-merger-app
✅ Status: Running and Healthy
✅ Port: 3000
✅ Access: http://localhost:3000

═══════════════════════════════════════════════════════
    IF YOU NEED TO REDEPLOY
═══════════════════════════════════════════════════════

Stop the app:
   docker-compose down

Start the app:
   docker-compose up -d

Restart the app:
   docker-compose restart

═══════════════════════════════════════════════════════
